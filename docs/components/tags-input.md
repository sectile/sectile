<!-- Generated by scripts/generate-component-pages.mjs. -->
# Tags Input

Tags Input separates editable draft state from accepted application value.

## Basic usage

Tags Input demonstrates skills as a separate, interactive configuration.

<ComponentExample component="tags-input" scenario="skills" title="Tags Input" description="Tags Input demonstrates skills as a separate, interactive configuration." :index="0" />

## Features

- Text draft.
- Token collection.
- Token cursor.
- Normalization.
- Controlled value and draft.

Each feature is part of the public semantic contract. Host packages may expose different input and rendering helpers while preserving accepted transitions.



## Example cases

### Limited

Bound the visible queue while preserving the order of pending feedback.

<ComponentExample component="tags-input" scenario="limited" title="Limited" description="Bound the visible queue while preserving the order of pending feedback." :index="1" />

### Controlled

The parent owns the current value and applies every accepted change back to Tags Input.

<ComponentExample component="tags-input" scenario="controlled" title="Controlled" description="The parent owns the current value and applies every accepted change back to Tags Input." :index="2" />

## Anatomy

Each labeled area is a public styling boundary. Select an area to inspect the DOM region and its stable data attributes.

<ComponentAnatomy component="tags-input" />

## State ownership

State that changes application-owned data supports controlled or uncontrolled ownership where the component contract permits it. Use uncontrolled state for local behavior and controlled state when a parent coordinates validation, persistence, or multiple controls.

## Disabled and readonly

Host projections distinguish disabled interaction from readonly inspection when the underlying control permits both. Disabled controls leave the interaction sequence; readonly controls remain available for inspection while rejecting mutation.

## Package availability

| Package | Import | Status |
| --- | --- | --- |
| Core | `@sectile/core/tags-input` | Published |
| DOM | `@sectile/dom/tags-input` | Published |
| Terminal | `@sectile/terminal/tags-input` | Published |
| Vue | `@sectile/vue/tags-input` | Workspace preview |

## Semantics

The component contract follows [Sectile composition theory](/theory/state-and-text#text). Visual styling remains outside the package; use host attributes, parts, and state projections as styling hooks.
