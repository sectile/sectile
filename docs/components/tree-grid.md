<!-- Generated by scripts/generate-component-pages.mjs. -->
# Tree Grid

Tree Grid composes cursor, selection, and collection projection semantics.

## Capabilities

- Tree row hierarchy.
- Grid cell cursor.
- Expansion.
- Selection.
- Edit mode.
- Eligibility.
- Controlled state.

## Package availability

| Package | Import | Status |
| --- | --- | --- |
| Core | `@sectile/core/tree-grid` | Published |
| DOM | `@sectile/dom/tree-grid` | Published |
| Terminal | `@sectile/terminal/tree-grid` | Published |
| Vue | `@sectile/vue/tree-grid` | Workspace preview |

## Usage

Import the public component subpath for the host you are building. Namespace imports keep the available state, events, controllers, and projection helpers together while the API is explored.

::: code-group

```ts [Core]
import * as treeGrid from '@sectile/core/tree-grid'
```

```ts [DOM]
import * as treeGrid from '@sectile/dom/tree-grid'
```

```ts [Terminal]
import * as treeGrid from '@sectile/terminal/tree-grid'
```

```ts [Vue]
import * as treeGrid from '@sectile/vue/tree-grid'
```

:::

## Supported scenarios

- Expanded.
- Collapsed.
- Unavailable cells.
- Controlled.

DOM and terminal scenarios refine the same semantic state. Host packages may expose different input and rendering helpers while preserving accepted transitions and controlled ownership.

## Interaction states

Component state that changes user-owned data supports controlled or uncontrolled ownership where listed above. Host projections distinguish disabled interaction from readonly inspection when the underlying control permits both.

## Semantics

The component contract follows [the relevant accessibility pattern](https://www.w3.org/WAI/ARIA/apg/patterns/treegrid/). Visual styling remains outside the package; use host attributes, parts, and state projections as styling hooks.
