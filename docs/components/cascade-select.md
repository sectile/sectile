<!-- Generated by scripts/generate-component-pages.mjs. -->
# Cascade Select

Cascade Select projects hierarchical choices into progressive selection columns.

## Basic usage

Cascade Select demonstrates location as a separate, interactive configuration.

<ComponentExample component="cascade-select" scenario="location" title="Cascade Select" description="Cascade Select demonstrates location as a separate, interactive configuration." :index="0" />

## Features

- Ordered tree domain.
- Progressive column projection.
- Branch navigation.
- Leaf selection.
- Eligibility.
- Controlled open highlight and value.

Each feature is part of the public semantic contract. Host packages may expose different input and rendering helpers while preserving accepted transitions.



## Example cases

### Disabled

Cascade Select is removed from keyboard and pointer interaction.

<ComponentExample component="cascade-select" scenario="disabled" title="Disabled" description="Cascade Select is removed from keyboard and pointer interaction." :index="1" />

### Controlled

The parent owns the current value and applies every accepted change back to Cascade Select.

<ComponentExample component="cascade-select" scenario="controlled" title="Controlled" description="The parent owns the current value and applies every accepted change back to Cascade Select." :index="2" />

## Anatomy

Each labeled area is a public styling boundary. Select an area to inspect the DOM region and its stable data attributes.

<ComponentAnatomy component="cascade-select" />

## State ownership

State that changes application-owned data supports controlled or uncontrolled ownership where the component contract permits it. Use uncontrolled state for local behavior and controlled state when a parent coordinates validation, persistence, or multiple controls.

## Disabled and readonly

Host projections distinguish disabled interaction from readonly inspection when the underlying control permits both. Disabled controls leave the interaction sequence; readonly controls remain available for inspection while rejecting mutation.

## Package availability

| Package | Import | Status |
| --- | --- | --- |
| Core | `@sectile/core/cascade-select` | Published |
| DOM | `@sectile/dom/cascade-select` | Published |
| Terminal | `@sectile/terminal/cascade-select` | Published |
| Vue | `@sectile/vue/cascade-select` | Workspace preview |

## Semantics

The component contract follows [Sectile composition theory](/theory/composition). Visual styling remains outside the package; use host attributes, parts, and state projections as styling hooks.
