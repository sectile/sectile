<!-- Generated by scripts/generate-component-pages.mjs. -->
# Spin Button

Spin Button models quantized values and host-independent range movement.

## Basic usage

Spin Button demonstrates integer as a separate, interactive configuration.

<ComponentExample component="spin-button" scenario="integer" title="Spin Button" description="Spin Button demonstrates integer as a separate, interactive configuration." :index="0" />

## Features

- Quantized decimal value.
- Text draft.
- Parse policy.
- Commit or cancel.
- Controlled value and draft.

Each feature is part of the public semantic contract. Host packages may expose different input and rendering helpers while preserving accepted transitions.



## Example cases

### Invalid draft

Spin Button demonstrates invalid draft as a separate, interactive configuration.

<ComponentExample component="spin-button" scenario="invalid-draft" title="Invalid draft" description="Spin Button demonstrates invalid draft as a separate, interactive configuration." :index="1" />

### Controlled

The parent owns the current value and applies every accepted change back to Spin Button.

<ComponentExample component="spin-button" scenario="controlled" title="Controlled" description="The parent owns the current value and applies every accepted change back to Spin Button." :index="2" />

## Anatomy

Each labeled area is a public styling boundary. Select an area to inspect the DOM region and its stable data attributes.

<ComponentAnatomy component="spin-button" />

## State ownership

State that changes application-owned data supports controlled or uncontrolled ownership where the component contract permits it. Use uncontrolled state for local behavior and controlled state when a parent coordinates validation, persistence, or multiple controls.

## Disabled and readonly

Host projections distinguish disabled interaction from readonly inspection when the underlying control permits both. Disabled controls leave the interaction sequence; readonly controls remain available for inspection while rejecting mutation.

## Package availability

| Package | Import | Status |
| --- | --- | --- |
| Core | `@sectile/core/spin-button` | Published |
| DOM | `@sectile/dom/spin-button` | Published |
| Terminal | `@sectile/terminal/spin-button` | Published |
| Vue | `@sectile/vue/spin-button` | Workspace preview |

## Semantics

The component contract follows [the relevant accessibility pattern](https://www.w3.org/WAI/ARIA/apg/patterns/spinbutton/). Visual styling remains outside the package; use host attributes, parts, and state projections as styling hooks.
