<!-- Generated by scripts/generate-component-pages.mjs. -->
# Rating

Rating composes ordered navigation, eligibility, and selection semantics.

## Basic usage

Rating demonstrates five star as a separate, interactive configuration.

<ComponentExample component="rating" scenario="five-star" title="Rating" description="Rating demonstrates five star as a separate, interactive configuration." :index="0" />

## Features

- Ordered score.
- Single selection.
- Clear policy.
- Controlled value.

Each feature is part of the public semantic contract. Host packages may expose different input and rendering helpers while preserving accepted transitions.



## Example cases

### Required

Rating demonstrates required as a separate, interactive configuration.

<ComponentExample component="rating" scenario="required" title="Required" description="Rating demonstrates required as a separate, interactive configuration." :index="1" />

### Controlled

The parent owns the current value and applies every accepted change back to Rating.

<ComponentExample component="rating" scenario="controlled" title="Controlled" description="The parent owns the current value and applies every accepted change back to Rating." :index="2" />

## Anatomy

Each labeled area is a public styling boundary. Select an area to inspect the DOM region and its stable data attributes.

<ComponentAnatomy component="rating" />

## State ownership

State that changes application-owned data supports controlled or uncontrolled ownership where the component contract permits it. Use uncontrolled state for local behavior and controlled state when a parent coordinates validation, persistence, or multiple controls.

## Disabled and readonly

Host projections distinguish disabled interaction from readonly inspection when the underlying control permits both. Disabled controls leave the interaction sequence; readonly controls remain available for inspection while rejecting mutation.

## Package availability

| Package | Import | Status |
| --- | --- | --- |
| Core | `@sectile/core/rating` | Published |
| DOM | `@sectile/dom/rating` | Published |
| Terminal | `@sectile/terminal/rating` | Published |
| Vue | `@sectile/vue/rating` | Workspace preview |

## Semantics

The component contract follows [the relevant accessibility pattern](https://www.w3.org/WAI/ARIA/apg/patterns/radio/). Visual styling remains outside the package; use host attributes, parts, and state projections as styling hooks.
