<!-- Generated by scripts/generate-component-pages.mjs. -->
# Tabs

Tabs composes ordered navigation, eligibility, and selection semantics.

## Basic usage

Tabs demonstrates manual as a separate, interactive configuration.

<ComponentExample component="tabs" scenario="manual" title="Tabs" description="Tabs demonstrates manual as a separate, interactive configuration." :index="0" />

## Features

- Cursor.
- Single selection.
- Manual or automatic activation.
- Orientation.
- Eligibility.

Each feature is part of the public semantic contract. Host packages may expose different input and rendering helpers while preserving accepted transitions.



## Example cases

### Automatic

Tabs demonstrates automatic as a separate, interactive configuration.

<ComponentExample component="tabs" scenario="automatic" title="Automatic" description="Tabs demonstrates automatic as a separate, interactive configuration." :index="1" />

### Vertical disabled

Tabs demonstrates vertical disabled as a separate, interactive configuration.

<ComponentExample component="tabs" scenario="vertical-disabled" title="Vertical disabled" description="Tabs demonstrates vertical disabled as a separate, interactive configuration." :index="2" />

## Anatomy

Each labeled area is a public styling boundary. Select an area to inspect the DOM region and its stable data attributes.

<ComponentAnatomy component="tabs" />

## State ownership

State that changes application-owned data supports controlled or uncontrolled ownership where the component contract permits it. Use uncontrolled state for local behavior and controlled state when a parent coordinates validation, persistence, or multiple controls.

## Disabled and readonly

Host projections distinguish disabled interaction from readonly inspection when the underlying control permits both. Disabled controls leave the interaction sequence; readonly controls remain available for inspection while rejecting mutation.

## Package availability

| Package | Import | Status |
| --- | --- | --- |
| Core | `@sectile/core/tabs` | Published |
| DOM | `@sectile/dom/tabs` | Published |
| Terminal | `@sectile/terminal/tabs` | Published |
| Vue | `@sectile/vue/tabs` | Workspace preview |

## Semantics

The component contract follows [the relevant accessibility pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/). Visual styling remains outside the package; use host attributes, parts, and state projections as styling hooks.
