# Vue playground

Interactive witnesses for `@sectile/vue` composition and styling hooks.

```sh
pnpm --filter @sectile/playground-vue dev
```

The playground is an independent workspace project. It imports only public
`@sectile/vue` subpaths and owns its build, typecheck, tests, and presentation.
