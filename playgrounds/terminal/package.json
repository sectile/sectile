{
  "name": "@sectile/playground-terminal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node src/main.mjs",
    "build": "node scripts/build.mjs",
    "test": "pnpm run build && node --test tests/*.test.mjs",
    "verify": "pnpm run test"
  },
  "dependencies": {
    "@sectile/core": "workspace:*",
    "@sectile/terminal": "workspace:*"
  }
}
