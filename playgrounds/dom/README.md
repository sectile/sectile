# DOM playground

Interactive tree-grid witness for the public DOM adapter.

```sh
pnpm --filter @sectile/playground-dom dev
```

Use arrow keys to move, `Alt+ArrowLeft/Right` to collapse and expand, Space to select, Enter to edit or commit, and Escape to cancel. Cell content remains host-owned while Sectile owns semantic state transitions.
