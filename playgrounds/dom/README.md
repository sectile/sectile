# DOM playground

Browser-based witnesses for every public `@sectile/dom` facade.

The vertical component rail selects a demo. Each scenario exposes its supported input, semantic state, and projected effects without adding framework or design-system behavior. URL hashes link directly to individual components.

Run with `pnpm --filter @sectile/playground-dom dev`.
