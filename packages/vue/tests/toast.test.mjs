import test from 'node:test'; import assert from 'node:assert/strict'; import { createSSRApp, h } from 'vue'; import { renderToString } from '@vue/server-renderer'; import { ToastClose, ToastDescription, ToastProvider, ToastRoot, ToastTitle, ToastViewport } from '../dist/toast.js';
test('Vue toast renders persistent native live-region composition', async () => { const app = createSSRApp({ render: () => h(ToastProvider, { initialToasts: [{ id: 'saved', title: 'Saved', description: 'Deployment complete', kind: 'success' }] }, { default: ({ toasts }) => h(ToastViewport, null, { default: () => toasts.map((toast) => h(ToastRoot, { value: toast.id }, { default: () => [h(ToastTitle), h(ToastDescription), h(ToastClose, null, { default: () => 'Close' })] })) }) }) }); const html = await renderToString(app); assert.match(html, /aria-live="polite"/); assert.match(html, /Saved/); assert.match(html, /Deployment complete/); assert.match(html, /Dismiss notification/); });
