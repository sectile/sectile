import type { Result, StableID } from '@sectile/primitives'; import { createSequence, type Sequence } from '@sectile/primitives/sequence'; import { applyCarouselEvent, createCarouselState, type CarouselCommand, type CarouselEvent, type CarouselPolicies, type CarouselState } from '@sectile/primitives/carousel'; import type { RevisionSnapshot } from '@sectile/primitives/revision'; import { createSemanticController, type SemanticController } from './internal/semantic-controller.js';
export interface CarouselOptions<ID extends StableID = StableID> { readonly root: HTMLElement; readonly slides: readonly ID[]; readonly defaultValue?: ID | null; readonly defaultPaused?: boolean; readonly policies?: CarouselPolicies; readonly onUpdate?: () => void }
export interface CarouselConnection<ID extends StableID = StableID> { getSnapshot(): RevisionSnapshot<CarouselState<ID>>; setSlideAttributes(element: HTMLElement, id: ID): void; handleEvent(event: CarouselEvent<ID>): boolean; disconnect(): void }
export function createCarousel<ID extends StableID>(options: CarouselOptions<ID>): Result<CarouselConnection<ID>> { const slides = createSequence(options.slides); if (!slides.ok) return slides; const runtime = createSemanticController<CarouselState<ID>, CarouselEvent<ID>, CarouselCommand<ID>, CarouselCommand<ID>>({ initial: createCarouselState(slides.value, options.defaultValue ?? options.slides[0] ?? null, options.defaultPaused ?? false), reducer: (state, event) => applyCarouselEvent(slides.value, state, event, options.policies), toEffect: (command) => command }); return runtime.ok ? { ok: true, value: new DOMCarousel(options, slides.value, runtime.value) } : runtime; }
class DOMCarousel<ID extends StableID> implements CarouselConnection<ID> { readonly #options: CarouselOptions<ID>; readonly #slides: Sequence<ID>; readonly #runtime: SemanticController<CarouselState<ID>, CarouselEvent<ID>, CarouselCommand<ID>>; readonly #elements = new Map<ID, HTMLElement>(); readonly #keydown: (event: KeyboardEvent) => void; public constructor(options: CarouselOptions<ID>, slides: Sequence<ID>, runtime: SemanticController<CarouselState<ID>, CarouselEvent<ID>, CarouselCommand<ID>>) { this.#options = options; this.#slides = slides; this.#runtime = runtime; this.#keydown = (event) => { const semantic = event.key === 'ArrowRight' ? 'next' : event.key === 'ArrowLeft' ? 'previous' : event.key === 'Home' ? 'first' : event.key === 'End' ? 'last' : null; if (semantic !== null) { this.handleEvent(semantic); event.preventDefault(); } }; options.root.addEventListener('keydown', this.#keydown); options.root.setAttribute('role', 'region'); options.root.setAttribute('aria-roledescription', 'carousel'); }
  public getSnapshot(): RevisionSnapshot<CarouselState<ID>> { return this.#runtime.getSnapshot(); } public setSlideAttributes(element: HTMLElement, id: ID): void { if (this.#slides.contains(id)) { this.#elements.set(id, element); this.#refresh(); } } public handleEvent(event: CarouselEvent<ID>): boolean { const result = this.#runtime.handle(event); if (result.ok) this.#refresh(); this.#options.onUpdate?.(); return true; } public disconnect(): void { this.#options.root.removeEventListener('keydown', this.#keydown); this.#elements.clear(); } #refresh(): void { const current = this.getSnapshot().state.cursor.current; for (const [id, element] of this.#elements) { element.setAttribute('role', 'group'); element.setAttribute('aria-roledescription', 'slide'); element.hidden = id !== current; } }
}
