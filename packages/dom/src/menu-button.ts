import type { Result, StableID } from '@sectile/core'; import { createMenuControl, type MenuControl, type MenuControlOptions } from './internal/menu-control.js'; export type MenuButtonOptions<ID extends StableID = StableID> = Omit<MenuControlOptions<ID>, 'kind'> & { readonly trigger: HTMLElement }; export type MenuButtonConnection<ID extends StableID = StableID> = MenuControl<ID>; export function createMenuButton<ID extends StableID>(options: MenuButtonOptions<ID>): Result<MenuButtonConnection<ID>> { return createMenuControl({ ...options, kind: 'menu-button' }); }
