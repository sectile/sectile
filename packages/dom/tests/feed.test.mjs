import assert from 'node:assert/strict'; import test from 'node:test'; import { unwrap } from '@sectile/primitives/result'; import { createFeed } from '../dist/feed.js'; test('DOM feed separates window requests from revision synchronization', () => { const root = new FakeElement(); let request = null; const feed = unwrap(createFeed({ root, items: ['a', 'b'], revision: 1, onRequestWindow: (...args) => { request = args; } })); feed.handleEvent('request-after'); assert.deepEqual(request, ['after', 'a', 1]); unwrap(feed.syncWindow({ items: ['b', 'c'], revision: 2, highlightedValue: 'b' })); assert.equal(feed.getSnapshot().state.revision, 2); assert.equal(feed.getSnapshot().state.pending, null); }); class FakeElement { attributes = new Map(); listeners = new Map(); setAttribute(n, v) { this.attributes.set(n, v); } addEventListener(t, l) { const s = this.listeners.get(t) ?? new Set(); s.add(l); this.listeners.set(t, s); } removeEventListener(t, l) { this.listeners.get(t)?.delete(l); } }
