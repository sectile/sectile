import assert from'node:assert/strict';import test from'node:test';import{unwrap}from'@sectile/core/result';import{createDialog}from'../dist/dialog.js';import{createPopover}from'../dist/popover.js';import{createAlertDialog}from'../dist/alert-dialog.js';import{createTooltip}from'../dist/tooltip.js';test('DOM popup facades preserve focus, announce, and visibility obligations',()=>{const trigger=new Fake(),root=new Fake();let focus=0;const dialog=createDialog({trigger,root,onInitialFocus:()=>focus++});trigger.emit('click');assert.equal(dialog.getSnapshot().state.open,true);assert.equal(focus,1);assert.equal(root.focused,true);assert.equal(root.attributes.get('role'),'dialog');const popoverRoot=new Fake(),popoverTrigger=new Fake();const popover=createPopover({root:popoverRoot,trigger:popoverTrigger});popoverTrigger.emit('click');assert.equal(popover.getSnapshot().state.open,true);assert.equal(popoverRoot.attributes.get('aria-modal'),'false');popoverTrigger.emit('keydown',{key:'Escape',preventDefault(){}});assert.equal(popover.getSnapshot().state.open,false);let announced=0;const alert=createAlertDialog({root:new Fake(),defaultOpen:false,onAnnounce:()=>announced++});alert.handleEvent('open');assert.equal(announced,1);const tipRoot=new Fake();const tip=createTooltip({root:tipRoot});tip.handleEvent('open');assert.equal(tipRoot.hidden,false)});test('DOM tooltip owns focus, hover, description linkage, and Escape',()=>{const root=new Fake(),trigger=new Fake();const tip=createTooltip({root,trigger,id:'help-tip'});assert.equal(trigger.attributes.get('aria-describedby'),'help-tip');trigger.emit('focus');assert.equal(tip.getSnapshot().state.open,true);trigger.emit('mouseenter');trigger.emit('blur');assert.equal(tip.getSnapshot().state.open,true);trigger.emit('mouseleave');assert.equal(tip.getSnapshot().state.open,false);trigger.emit('focus');trigger.emit('keydown',{key:'Escape',preventDefault(){}});assert.equal(tip.getSnapshot().state.open,false)});test('DOM layer manager routes Escape only to the topmost popup',()=>{const ownerDocument={};const lowerRoot=new Fake(ownerDocument),lowerTrigger=new Fake(ownerDocument),upperRoot=new Fake(ownerDocument),upperTrigger=new Fake(ownerDocument);const lower=createDialog({root:lowerRoot,trigger:lowerTrigger});const upper=createDialog({root:upperRoot,trigger:upperTrigger});lower.handleEvent('open');upper.handleEvent('open');lowerTrigger.emit('keydown',{key:'Escape',preventDefault(){}});assert.equal(lower.getSnapshot().state.open,true);assert.equal(upper.getSnapshot().state.open,true);upperTrigger.emit('keydown',{key:'Escape',preventDefault(){}});assert.equal(upper.getSnapshot().state.open,false);lowerTrigger.emit('keydown',{key:'Escape',preventDefault(){}});assert.equal(lower.getSnapshot().state.open,false)});test('DOM layer manager closes nested descendants but preserves independent layers',()=>{const ownerDocument={};const outerRoot=new Fake(ownerDocument),outerTrigger=new Fake(ownerDocument),nestedRoot=new Fake(ownerDocument),nestedTrigger=new Fake(ownerDocument),independentRoot=new Fake(ownerDocument),independentTrigger=new Fake(ownerDocument);outerRoot.children.add(nestedTrigger);const outer=createDialog({root:outerRoot,trigger:outerTrigger});const nested=createPopover({root:nestedRoot,trigger:nestedTrigger});const independent=createDialog({root:independentRoot,trigger:independentTrigger});outer.handleEvent('open');nested.handleEvent('open');outer.handleEvent('close');assert.equal(outer.getSnapshot().state.open,false);assert.equal(nested.getSnapshot().state.open,false);outer.handleEvent('open');independent.handleEvent('open');outer.handleEvent('close');assert.equal(outer.getSnapshot().state.open,false);assert.equal(independent.getSnapshot().state.open,true)});class Fake{attributes=new Map();listeners=new Map();children=new Set();hidden=false;focused=false;id='';tabIndex=-1;style={};dataset={};constructor(ownerDocument){this.ownerDocument=ownerDocument}setAttribute(n,v){this.attributes.set(n,v)}addEventListener(t,l){const s=this.listeners.get(t)??new Set();s.add(l);this.listeners.set(t,s)}removeEventListener(t,l){this.listeners.get(t)?.delete(l)}emit(t,e={}){for(const l of this.listeners.get(t)??[])l(e)}contains(target){return target===this||this.children.has(target)}querySelectorAll(){return[]}focus(){this.focused=true}}
