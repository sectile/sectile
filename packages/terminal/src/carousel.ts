import type { Result, StableID } from '@sectile/primitives'; import { createSequence } from '@sectile/primitives/sequence'; import { applyCarouselEvent, createCarouselState, type CarouselCommand, type CarouselEvent, type CarouselPolicies, type CarouselState } from '@sectile/primitives/carousel'; import type { RevisionSnapshot } from '@sectile/primitives/revision'; import type { TerminalKeyboardInput } from './keyboard.js'; import { createSemanticController, type SemanticController } from './internal/semantic-controller.js';
export interface CarouselOptions<ID extends StableID = StableID> { readonly slides: readonly ID[]; readonly defaultValue?: ID | null; readonly defaultPaused?: boolean; readonly policies?: CarouselPolicies; readonly onUpdate?: () => void } export interface CarouselConnection<ID extends StableID = StableID> { getSnapshot(): RevisionSnapshot<CarouselState<ID>>; handleEvent(event: CarouselEvent<ID>): boolean; handleKeyboardInput(input: TerminalKeyboardInput): boolean }
export function createCarousel<ID extends StableID>(options: CarouselOptions<ID>): Result<CarouselConnection<ID>> { const slides = createSequence(options.slides); if (!slides.ok) return slides; const runtime = createSemanticController<CarouselState<ID>, CarouselEvent<ID>, CarouselCommand<ID>, CarouselCommand<ID>>({ initial: createCarouselState(slides.value, options.defaultValue ?? options.slides[0] ?? null, options.defaultPaused ?? false), reducer: (state, event) => applyCarouselEvent(slides.value, state, event, options.policies), toEffect: (command) => command }); return runtime.ok ? { ok: true, value: new TerminalCarousel(options, runtime.value) } : runtime; }
class TerminalCarousel<ID extends StableID> implements CarouselConnection<ID> { readonly #options: CarouselOptions<ID>; readonly #runtime: SemanticController<CarouselState<ID>, CarouselEvent<ID>, CarouselCommand<ID>>; public constructor(options: CarouselOptions<ID>, runtime: SemanticController<CarouselState<ID>, CarouselEvent<ID>, CarouselCommand<ID>>) { this.#options = options; this.#runtime = runtime; } public getSnapshot(): RevisionSnapshot<CarouselState<ID>> { return this.#runtime.getSnapshot(); } public handleEvent(event: CarouselEvent<ID>): boolean { this.#runtime.handle(event); this.#options.onUpdate?.(); return true; } public handleKeyboardInput(input: TerminalKeyboardInput): boolean { const event = input.key === 'right' ? 'next' : input.key === 'left' ? 'previous' : input.key === 'home' ? 'first' : input.key === 'end' ? 'last' : input.key === 'space' ? 'toggle-pause' : null; return event === null ? false : this.handleEvent(event); } }
