import type { Result, StableID } from '@sectile/primitives'; import { createSequence, type Sequence } from '@sectile/primitives/sequence'; import { applyCarouselEvent, createCarouselState, type CarouselCommand, type CarouselEvent, type CarouselPolicies, type CarouselState } from '@sectile/primitives/carousel'; import type { RevisionSnapshot } from '@sectile/primitives/revision'; import type { TerminalKeyboardInput } from './keyboard.js'; import { createSemanticController, type SemanticController } from './internal/semantic-controller.js';
export interface CarouselOptions<ID extends StableID = StableID>{readonly slides:readonly ID[];readonly value?:ID|null;readonly defaultValue?:ID|null;readonly paused?:boolean;readonly defaultPaused?:boolean;readonly policies?:CarouselPolicies;readonly onValueChange?:(value:ID|null)=>void;readonly onPausedChange?:(paused:boolean)=>void;readonly onAnnounce?:(id:ID)=>void;readonly onUpdate?:()=>void}export interface CarouselControlledValues<ID extends StableID=StableID>{readonly value?:ID|null;readonly paused?:boolean}export interface CarouselConnection<ID extends StableID=StableID>{getSnapshot():RevisionSnapshot<CarouselState<ID>>;syncControlledValues(values:CarouselControlledValues<ID>):Result<RevisionSnapshot<CarouselState<ID>>>;handleEvent(event:CarouselEvent<ID>):boolean;handleKeyboardInput(input:TerminalKeyboardInput):boolean}
export function createCarousel<ID extends StableID>(options:CarouselOptions<ID>):Result<CarouselConnection<ID>>{const slides=createSequence(options.slides);if(!slides.ok)return slides;const vc=options.value!==undefined,pc=options.paused!==undefined;const runtime=createSemanticController<CarouselState<ID>,CarouselEvent<ID>,CarouselCommand<ID>,CarouselCommand<ID>>({initial:createCarouselState(slides.value,options.value!==undefined?options.value:options.defaultValue??options.slides[0]??null,options.paused??options.defaultPaused??false),reducer:(state,event)=>applyCarouselEvent(slides.value,state,event,options.policies),reconcile:(previous,proposed)=>createCarouselState(slides.value,vc?previous.cursor.current:proposed.cursor.current,pc?previous.paused:proposed.paused),notify:(previous,proposed)=>{if(previous.cursor.current!==proposed.cursor.current)options.onValueChange?.(proposed.cursor.current);if(previous.paused!==proposed.paused)options.onPausedChange?.(proposed.paused)},toEffect:(command)=>command});return runtime.ok?{ok:true,value:new TerminalCarousel(options,slides.value,runtime.value,vc,pc)}:runtime}
class TerminalCarousel<ID extends StableID>implements CarouselConnection<ID>{readonly #options:CarouselOptions<ID>;readonly #slides:Sequence<ID>;readonly #runtime:SemanticController<CarouselState<ID>,CarouselEvent<ID>,CarouselCommand<ID>>;readonly #vc:boolean;readonly #pc:boolean;public constructor(options:CarouselOptions<ID>,slides:Sequence<ID>,runtime:SemanticController<CarouselState<ID>,CarouselEvent<ID>,CarouselCommand<ID>>,vc:boolean,pc:boolean){this.#options=options;this.#slides=slides;this.#runtime=runtime;this.#vc=vc;this.#pc=pc}public getSnapshot(){return this.#runtime.getSnapshot()}public syncControlledValues(values:CarouselControlledValues<ID>){if(this.#vc!==(values.value!==undefined)||this.#pc!==(values.paused!==undefined))return{ok:false as const,error:{class:'construction' as const,code:'controlled-shape-mismatch',message:'Controlled carousel values must preserve their construction-time shape.'}};const state=this.getSnapshot().state;const result=this.#runtime.replace(createCarouselState(this.#slides,this.#vc?values.value as ID|null:state.cursor.current,this.#pc?values.paused as boolean:state.paused));if(result.ok)this.#options.onUpdate?.();return result}public handleEvent(event:CarouselEvent<ID>){const result=this.#runtime.handle(event);if(result.ok)for(const command of result.commands)this.#options.onAnnounce?.(command.id);this.#options.onUpdate?.();return true}public handleKeyboardInput(input:TerminalKeyboardInput){const event=input.key==='right'?'next':input.key==='left'?'previous':input.key==='home'?'first':input.key==='end'?'last':input.key==='space'?'toggle-pause':null;return event===null?false:this.handleEvent(event)}}
