import type { Result } from '@sectile/primitives'; import { applySwitchEvent, createSwitchState, type SwitchCommand, type SwitchEvent, type SwitchState } from '@sectile/primitives/switch'; import { createTerminalCheckedControl, type TerminalCheckedControl } from './internal/checked-control.js';
export interface SwitchOptions { readonly checked?: boolean; readonly defaultChecked?: boolean; readonly onCheckedChange?: (checked: boolean) => void; readonly onUpdate?: () => void }
export type SwitchConnection = TerminalCheckedControl<SwitchState, SwitchEvent, boolean>;
export function createSwitch(options: SwitchOptions = {}): Result<SwitchConnection> { return createTerminalCheckedControl<SwitchState, SwitchEvent, SwitchCommand, boolean>({ controlled: options.checked !== undefined, initial: createSwitchState(options.checked ?? options.defaultChecked ?? false), toggleEvent: 'toggle', reducer: applySwitchEvent, create: createSwitchState, read: (state) => state.checked, onChange: options.onCheckedChange, onUpdate: options.onUpdate }); }
