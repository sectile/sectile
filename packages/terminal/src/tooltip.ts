import type{Result}from'@sectile/primitives';import{applyTooltipEvent,createTooltipState,type TooltipCommand,type TooltipEvent,type TooltipState}from'@sectile/primitives/tooltip';import{createTerminalPopup,type TerminalPopupConnection}from'./internal/popup-control.js';export interface TooltipOptions{readonly open?:boolean;readonly defaultOpen?:boolean;readonly onUpdate?:()=>void}export type TooltipConnection=TerminalPopupConnection<TooltipState,TooltipEvent>;export function createTooltip(o:TooltipOptions={}):Result<TooltipConnection>{return createTerminalPopup<TooltipState,TooltipEvent,TooltipCommand>({initial:createTooltipState(o.open??o.defaultOpen??false),reducer:applyTooltipEvent,close:'close',onUpdate:o.onUpdate})}
