import type { Result, StableID } from '@sectile/primitives'; import type { RevisionSnapshot } from '@sectile/primitives/revision'; import type { TreeNodeInput } from '@sectile/primitives/tree'; import { applyMenuEvent, createMenuModel, createMenuState, type MenuCommand, type MenuEvent, type MenuState } from '@sectile/primitives/menu'; import type { TerminalKeyboardInput } from '../keyboard.js'; import { createSemanticController, type SemanticController } from './semantic-controller.js';
export type MenuKind = 'menu' | 'menubar' | 'menu-button'; export interface MenuControlOptions<ID extends StableID> { readonly items: readonly TreeNodeInput<ID>[]; readonly kind: MenuKind; readonly defaultOpen?: boolean; readonly defaultHighlightedValue?: ID | null; readonly onInvoke?: (id: ID) => void; readonly onUpdate?: () => void } export interface MenuControl<ID extends StableID> { getSnapshot(): RevisionSnapshot<MenuState<ID>>; handleEvent(event: MenuEvent<ID>): boolean; handleKeyboardInput(input: TerminalKeyboardInput): boolean }
export function createMenuControl<ID extends StableID>(options: MenuControlOptions<ID>): Result<MenuControl<ID>> { const model = createMenuModel(options.items); if (!model.ok) return model; const runtime = createSemanticController<MenuState<ID>, MenuEvent<ID>, MenuCommand<ID>, MenuCommand<ID>>({ initial: createMenuState(model.value.tree, options.defaultOpen ?? options.kind !== 'menu-button', options.defaultHighlightedValue ?? null, []), reducer: (state, event) => applyMenuEvent(model.value.tree, state, event), toEffect: (command) => command }); return runtime.ok ? { ok: true, value: new TerminalMenuControl(options, runtime.value) } : runtime; }
class TerminalMenuControl<ID extends StableID> implements MenuControl<ID> { readonly #options: MenuControlOptions<ID>; readonly #runtime: SemanticController<MenuState<ID>, MenuEvent<ID>, MenuCommand<ID>>; public constructor(options: MenuControlOptions<ID>, runtime: SemanticController<MenuState<ID>, MenuEvent<ID>, MenuCommand<ID>>) { this.#options = options; this.#runtime = runtime; } public getSnapshot(): RevisionSnapshot<MenuState<ID>> { return this.#runtime.getSnapshot(); } public handleEvent(event: MenuEvent<ID>): boolean { const result = this.#runtime.handle(event); if (result.ok) for (const effect of result.commands) if (effect.type === 'invoke') this.#options.onInvoke?.(effect.id); this.#options.onUpdate?.(); return true; } public handleKeyboardInput(input: TerminalKeyboardInput): boolean { const event = toMenuEvent(input, this.#options.kind); return event === null ? false : this.handleEvent(event); } }
function toMenuEvent(input: TerminalKeyboardInput, kind: MenuKind): Extract<MenuEvent, string> | null { if (input.altKey || input.ctrlKey) return null; if (input.key === 'escape') return 'escape'; if (input.key === 'enter' || input.key === 'space') return 'invoke'; if (input.key === 'down') return kind === 'menubar' ? 'open-submenu' : 'next'; if (input.key === 'up') return 'previous'; if (input.key === 'right') return kind === 'menubar' ? 'next' : 'open-submenu'; if (input.key === 'left') return kind === 'menubar' ? 'previous' : 'close-submenu'; return null; }
