import type { Result, StableID } from '@sectile/core'; import { createMenuControl, type MenuControl, type MenuControlOptions } from './internal/menu-control.js'; export type MenubarOptions<ID extends StableID = StableID> = Omit<MenuControlOptions<ID>, 'kind'>; export type MenubarConnection<ID extends StableID = StableID> = MenuControl<ID>; export function createMenubar<ID extends StableID>(options: MenubarOptions<ID>): Result<MenubarConnection<ID>> { return createMenuControl({ ...options, kind: 'menubar' }); }
