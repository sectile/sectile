import type { Result, StableID } from './shared.js'; import type { Sequence } from './structures/sequence.js'; import { fail, ok } from './internal/kernel/foundation.js'; import { createMachineUpdate } from './internal/kernel/machine.js'; import { createCursorState, type CursorState } from './internal/state/cursor.js';
export interface CarouselState<ID extends StableID = StableID> { readonly cursor: CursorState<ID>; readonly paused: boolean }
export type CarouselEvent<ID extends StableID = StableID> = 'next' | 'previous' | 'first' | 'last' | 'pause' | 'resume' | 'toggle-pause' | { readonly type: 'focus'; readonly id: ID };
export type CarouselCommand<ID extends StableID = StableID> = { readonly type: 'announce-slide'; readonly id: ID };
export interface CarouselPolicies { readonly wrap?: boolean }
export interface CarouselUpdate<ID extends StableID = StableID> { readonly state: CarouselState<ID>; readonly commands: readonly CarouselCommand<ID>[] }
export function createCarouselState<ID extends StableID>(slides: Sequence<ID>, current: ID | null = null, paused = false): Result<CarouselState<ID>> { if (current !== null && !slides.contains(current)) return fail('construction', 'carousel-cursor-outside-slides', 'Carousel cursor must identify a slide.'); return ok(Object.freeze({ cursor: createCursorState(current), paused })); }
export function applyCarouselEvent<ID extends StableID>(slides: Sequence<ID>, state: CarouselState<ID>, event: CarouselEvent<ID>, policies: CarouselPolicies = {}): Result<CarouselUpdate<ID>> { const valid = createCarouselState(slides, state.cursor.current, state.paused); if (!valid.ok) return { ok: false, error: { ...valid.error, class: 'transition-rejection' } }; if (event === 'pause' || event === 'resume' || event === 'toggle-pause') { const paused = event === 'pause' ? true : event === 'resume' ? false : !state.paused; return createMachineUpdate(Object.freeze({ cursor: state.cursor, paused })); } let id: ID | null; if (typeof event === 'object') { if (!slides.contains(event.id)) return fail('transition-rejection', 'carousel-target-unavailable', 'Direct carousel focus requires a slide.'); id = event.id; } else if (event === 'first') id = slides.at(0); else if (event === 'last') id = slides.at(slides.size - 1); else if (state.cursor.current === null) id = slides.at(event === 'next' ? 0 : slides.size - 1); else { const moved = slides.move(state.cursor.current, event === 'next' ? 1 : -1, policies.wrap === false ? 'stop' : 'wrap'); id = moved.kind === 'found' ? moved.id : state.cursor.current; } if (id === null || id === state.cursor.current) return createMachineUpdate(state); return createMachineUpdate(Object.freeze({ cursor: createCursorState(id), paused: state.paused }), [{ type: 'announce-slide', id }]); }
