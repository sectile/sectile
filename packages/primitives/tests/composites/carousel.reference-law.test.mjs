import assert from 'node:assert/strict'; import test from 'node:test'; import { createSequence } from '../../.verification-dist/structures/sequence.js'; import { createCarouselState, applyCarouselEvent } from '../../.verification-dist/carousel.js'; import { createReferenceCarouselState, applyReferenceCarouselEvent } from '../../.verification-dist/internal/reference/composites/carousel.js'; import { unwrap } from '../support.mjs';
test('carousel movement and pause authority match an independent reference', () => { const slides = unwrap(createSequence(['a', 'b', 'c'])); for (const current of [null, 'a', 'b', 'c']) for (const paused of [false, true]) for (const event of ['next', 'previous', 'first', 'last', 'pause', 'resume', 'toggle-pause', { type: 'focus', id: 'b' }]) { const actual = applyCarouselEvent(slides, unwrap(createCarouselState(slides, current, paused)), event); const expected = applyReferenceCarouselEvent(slides, createReferenceCarouselState(current, paused), event); assert.deepEqual(observe(actual), observeRef(expected)); } }); function observe(r) { return r.ok ? { ok: true, state: r.value.state, commands: r.value.commands } : { ok: false, errorClass: r.error.class, errorCode: r.error.code }; } function observeRef(r) { return r.ok ? { ok: true, state: r.value.state, commands: r.value.commands } : { ok: false, errorClass: r.errorClass, errorCode: r.errorCode }; }
