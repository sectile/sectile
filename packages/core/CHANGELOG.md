# @sectile/core

## Unreleased

### Minor Changes

- Add generation-bound Form validation and submission results.
- Add finite collection-window, nested layer-stack, and identity-based sequence/tree reorder theories.
- Refactor Feed window synchronization onto the shared revision and request-generation contract.
- Require pending Feed replacements and Form submission completions to return their originating generation token.
- Add DOM, terminal, and Vue reorder hosts, document-scoped popup ownership, terminal layer scopes, and host-input evidence ratchets.

## 0.1.0

### Minor Changes

- Publish the initial renderer-neutral core and DOM and terminal host adapters.
