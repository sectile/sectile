import { unwrap } from '../../result.js';
import type{Result}from'../../shared.js';import{fail,ok}from'../kernel/foundation.js';import{createMachineUpdate}from'../kernel/machine.js';
export interface PopupState{readonly open:boolean}export type PopupEvent='open'|'close'|'toggle'|{readonly type:'set-open';readonly open:boolean};export type PopupCommand={readonly type:'popup-opened'}|{readonly type:'popup-closed'};export interface PopupUpdate{readonly state:PopupState;readonly commands:readonly PopupCommand[]}
export function createPopupState(open=false):PopupState{return unwrap(tryCreatePopupState(open))}
export function tryCreatePopupState(open=false):Result<PopupState>{return typeof open==='boolean'?ok(Object.freeze({open})):fail('construction','invalid-popup-state','Popup open state must be boolean.')}
export function applyPopupEvent(state:PopupState,event:PopupEvent):Result<PopupUpdate>{if(typeof state.open!=='boolean')return fail('transition-rejection','invalid-popup-state','Popup open state must be boolean.');const next=typeof event==='object'?event.type==='set-open'&&typeof event.open==='boolean'?event.open:null:event==='toggle'?!state.open:event==='open'?true:event==='close'?false:null;if(next===null)return fail('transition-rejection','invalid-popup-event','Popup event is not accepted.');if(next===state.open)return createMachineUpdate(state);return createMachineUpdate(Object.freeze({open:next}),[{type:next?'popup-opened':'popup-closed'}])}
